// script llenado dinamico
// script accordion

let myAlerts = {
    llenadoAlert(){
        botones.forEach((val, id) => {
            
            return `<h1>${val}</h1>`
        });
    }

}